// JavaScript is an interpreted language where souce code is executed directly by an interpreter, line by line, without any prior compilation into the machine code.

// It contrasts with compiled language where the source code is translated into a machine code at once before execution.



// Dynamically Typed Language =>
    // A dynamically typed language is a programming language where the type of variable is determined at runtime, rather than during compilation.



// Statically Typed Language =>
    // A statically typed language is one where the data type of variable is known at compile time instead of runtime.