console.log("@@@@@@@@@@@ Logical Operators @@@@@@@@@@@");

    // && (Logical AND), if both are true in that situation only condition is true, otherwise always false
    // || (Logical OR), if any one condition is true than condition is true, if both false only in that condn false
    // ! (Logical NOT), true converts false and vice versa

    let x = 5;
    let y = 6;

    let cond1 = x === 5;        // true
    let cond2 = y > x;          // true

    console.log("cond1 && cond2 = ", cond1 && cond2);   // true

console.log("@@@@@@@@@@@ Logical Operators @@@@@@@@@@@");