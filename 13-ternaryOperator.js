// ✅ ############## Ternary Operator ############## ✅ //

    // Ternary Operators is short hand of if-else statement, where it contains condition, ifExpression & elseExpression. 
    // If condition is true then ifExpression going to executes and if condition is false the eleExpression is going to executes.

    let age = 34;

    let eligibleMsg = age >= 18 ? "Eligible" : "Not eligible";

    console.log("Your eligible status on age entered ->>", eligibleMsg);

// ✅ ############## Ternary Operator ############## ✅ //


// ✅ ############## Ternary Operator (Examples) ############## ✅ //

    // 1 =>
    // let num = 0;
    
    // let result = num >= 0 ? "positive" : "negative";
    // console.log(result);


    // 2 =>
        // let num = 4;
        
        // let result = (num%2 == 0) ? "Even" : "Odd";
        // console.log(result);


    // 3 =>
        // age = 35;

        // let canVote = age >= 18? "Yes" : "No";
        // console.log(canVote);


    // 4 =>
        // let username;

        // let displayName = username ? username : "Default";
        // console.log(displayName);


    // 5 =>
        // let score = 55;

        // let grade = score >= 90 ? "A" :
        //                     score >= 80 ? "B" :
        //                     score >= 70 ? "C" :
        //                     score >= 60 ? "D" : "F";

        // console.log(grade);


    // 6 =>
        // let isOn = true;

        // isOn = isOn ? false : true;
        // console.log(isOn);


    // 7 =>
        // let isLoggedIn = false;

        // console.log(`You are ${ isLoggedIn? "" : "not " }loggedIn...`);


    // 8 =>
        // let count = 11;

        // let text = `${count} item${count === 1 ? "" : "s"}`;
        // console.log(text);
        
// ✅ ############## Ternary Operator (Examples) ############## ✅ //