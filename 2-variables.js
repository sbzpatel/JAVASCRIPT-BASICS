// ✅ ############## JavaScript Variable ############## ✅ //

    // Variable is a named container that store and manage different types of data(like number, text, object, array) use for further operations throughout the program.

    // Assignment operator(=) used to store a value in a variable.

    // Eg:
        fullName = "Shahbaz Patel";
        age = 34;
        price = 101.56;
        x = null;
        y = undefined;
        isMarried = true;
        totalMarks = 555;
        $price = 405;

        // 123friend = "Kaleem";    // Wrong, variable can not starts with numeric
        friend1 = "Shahbaz";        // Right, variable can followed by numeric

        // fullName = 35;       // dynamic type where we can change the fullName with any type of value

        {
            console.log($price);
        }

        magicNum = age * totalMarks;  
        // here we used "age" and "totalMarks" variables for further operations and assigned obtained value to the variable "magicNum"

        console.log(magicNum);

        console.log(fullName);
        console.log($price);
        
// ✅ ############## JavaScript Variable ############## ✅ //